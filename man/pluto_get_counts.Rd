% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/files.R
\name{pluto_get_counts}
\alias{pluto_get_counts}
\title{Get counts / normalized expression as a data frame}
\usage{
pluto_get_counts(experiment_id, kind = "raw", dest_dir = tempdir())
}
\arguments{
\item{experiment_id}{Pluto experiment ID.}

\item{kind}{One of "raw", "cpm", "tpm_gene", "tpm_transcript" (default "raw").}

\item{dest_dir}{Where to save intermediate downloads. Defaults to \code{tempdir()}.}
}
\value{
A data.frame matching the file as stored on Pluto (no transpose).
}
\description{
Unified accessor for the bulk expression matrices Pluto stores:
\itemize{
\item \code{kind = "raw"} — raw assay data (counts). Equivalent to
\code{pluto_read_assay_data()}.
\item \code{kind = "cpm"} — CPM-normalized assay data.
\item \code{kind = "tpm_gene"} — TPM by gene.
\item \code{kind = "tpm_transcript"} — TPM by transcript.
}

For \code{"raw"} this delegates to the existing assay-data flow. Other kinds
discover the right file through \verb{/files/?data_type=<X>}, download via a
signed URL, and parse the CSV.
}
