% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflows.R
\name{pluto_wait_for_workflow}
\alias{pluto_wait_for_workflow}
\title{Wait for a workflow to reach a terminal status}
\usage{
pluto_wait_for_workflow(
  experiment_id,
  workflow_uuid,
  poll_interval = 10,
  timeout = 3 * 60 * 60,
  progress = FALSE
)
}
\arguments{
\item{experiment_id}{Pluto experiment ID.}

\item{workflow_uuid}{Workflow UUID.}

\item{poll_interval}{Seconds between polls (default 10).}

\item{timeout}{Maximum seconds to wait. Defaults to 3 hours (Nextflow
pipelines are slow); pass \code{Inf} to wait indefinitely.}

\item{progress}{If TRUE, emit a message on each status transition.}
}
\value{
The workflow record at the last poll.
}
\description{
Polls the workflow detail endpoint until \code{status} is one of
\code{completed}, \code{accepted}, or \code{failed}. Does not raise on workflow
failure — check \verb{$status} after return. Raises only on timeout
expiry.
}
